from string import ascii_lowercase


def solution(age):
    
    a = list(ascii_lowercase)
    
    ans = list(str(age))
    
    ans_1 = [int(j) for j in ans]
    
    answer = ''
    for j in ans_1:
        answer += a[j]
    return answer
    
    
    
