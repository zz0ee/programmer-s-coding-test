def solution(arr, divisor):
    answer = []
    for i in arr:
        if i%divisor == 0:
            answer.append(i)
    if answer == []:
        result = [-1]
        return result
    else:
        answer.sort()
        return answer
    
    
