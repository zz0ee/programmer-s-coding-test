def solution(absolutes, signs):
    for i in range(len(signs)):
        if signs[i] == True:
            pass
        else:
            absolutes[i] = -absolutes[i]
    return sum(absolutes)
        
        
