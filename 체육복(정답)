def solution(n, lost, reserve):
    set_r = set(reserve) - set(lost)
    set_l = set(lost) - set(reserve)

    for r in set_r:
        if r-1 in set_l:
            set_l.remove(r-1)
        elif r+1 in set_l:
            set_l.remove(r+1)

    return n - len(set_l)
